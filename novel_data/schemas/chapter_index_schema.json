{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Chapter Index Schema",
    "description": "Schema for the index.json file within a chapter's root directory.",
    "type": "object",
    "required": ["title", "summary"],
    "properties": {
        "title": {
            "type": "string",
            "description": "The title of the chapter."
        },
        "slug": {
            "type": "string",
            "description": "A URL-friendly identifier for the chapter."
        },
        "summary": {
            "type": "string",
            "description": "A brief summary of the chapter."
        }
    }
}