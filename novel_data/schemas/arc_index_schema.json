{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Arc Index Schema",
    "description": "Schema for the index.json file within an arc's root directory.",
    "type": "object",
    "required": ["title"],
    "properties": {
        "title": {
            "type": "string",
            "description": "The title of the story arc."
        },
        "slug": {
            "type": "string",
            "description": "A URL-friendly identifier for the story arc."
        },
        "summary": {
            "type": "string",
            "description": "A brief summary of the arc."
        }
    }
}